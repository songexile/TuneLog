import { StyleSheet } from "react-native";

const bgButton = "9D3BEA";

//global styles for react-native, this contains all the global styles for the app

export default StyleSheet.create({
  bgPurple: { backgroundColor: "#D3A8F6" },
  buttonColor: {
    backgroundColor: "#9D3BEA",
  },
});
