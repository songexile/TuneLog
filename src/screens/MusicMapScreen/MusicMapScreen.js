import { View, Text } from "react-native";
import React from "react";

//To be added in the future as the music map screen.

const MusicMapScreen = ({ navigation }) => {
  return (
    <View>
      <Text>MusicMapScreen</Text>
    </View>
  );
};

export default MusicMapScreen;
